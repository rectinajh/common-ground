# Somnia × DreamDEX Shannon testnet configuration (chainId 50312).
# Defaults are already correct; only the private key is required for writes.

DREAMDEX_INDEXER_URL=https://dev.smk.somnia.host/v1/graphql
DREAMDEX_WS_RPC_URL=wss://api.infra.testnet.somnia.network/ws

# REQUIRED only for write operations (mint / burn / redeem / faucet).
# Use a dedicated test wallet; never commit a real key.
DREAMDEX_PRIVATE_KEY=
