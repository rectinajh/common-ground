# Shannon testnet
RPC_URL=https://dream-rpc.somnia.network

# The CommonGroundCampaign to keep advancing.
CAMPAIGN_ADDRESS=0x0000000000000000000000000000000000000000

# Any funded testnet key. This key only calls PERMISSIONLESS advance functions
# (activateBase / syncMarketAndBonus / expireTask / failFunding) — it cannot
# move funds to an arbitrary address.
PRIVATE_KEY=
